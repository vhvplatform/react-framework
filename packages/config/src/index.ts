// Re-export configuration files for programmatic use
export const version = '1.0.0';

export { default as eslintConfig } from '../eslint.config.js';
export { default as prettierConfig } from '../prettier.config.js';
